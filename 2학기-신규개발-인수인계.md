# 📋 2학기 학급 자치 시스템 — 신규 개발 인수인계 문서

> **새 채팅창에서 이 문서 전체를 첫 메시지에 붙여넣고 시작하세요.**
> 1학기 시스템과 데이터는 그대로 두고, 새로 처음부터 만드는 프로젝트입니다.

---

## 0. 이 문서의 역할

- **목적**: 1학기 채팅에서 합의된 모든 알고리즘·결정사항을 새 채팅에 손실 없이 전달
- **사용법**: 새 채팅에서 "이 명세대로 개발 시작해줘"라고 지시
- **원칙**:
  - 기존 Firebase 프로젝트(`w-program`) **절대 건드리지 않음**
  - GitHub 리포는 포크 또는 새 브랜치
  - 새 Firebase 프로젝트 별도 생성 (예: `w-program-2`)

---

## 1. 프로젝트 컨텍스트

| 항목 | 내용 |
|---|---|
| 사용자 | 초등학교 5학년 담임 선생님 (`led13@naver.com`) |
| 학생 | 25명 (남녀 혼합) |
| 배포 환경 | **Google Apps Script (GAS)** Web App — `script.google.com/macros/.../exec` |
| 1학기 리포 | `https://github.com/codechord123/smartclass-claude-` (브랜치 `claude/fix-post-loading-issue-1HWBo`) |
| 1학기 Firebase | `w-program` (그대로 둠) |
| 1학기 데이터 | 거북이 독서 JSON 백업 완료, 별도 보관 |

---

## 2. 기술 스택 (1학기와 동일하게 유지)

- **단일 HTML 파일** (`index.html`) 내 모든 코드 (React + Tailwind + Firebase)
- **React 18** via `esm.sh/react@18.2.0` (importmap 사용)
- **Babel 7.23.10 고정** (in-browser 변환, 자동 업그레이드 금지)
- **Tailwind CDN**
- **Firebase v10** (Auth + Firestore)
- **Apps Script**로 HTML 호스팅

### 필수 HTML 헤드 구성 (1학기 학습 결과)

```html
<script src="https://unpkg.com/@babel/standalone@7.23.10/babel.min.js"></script>
<script type="importmap">
{
  "imports": {
    "react": "https://esm.sh/react@18.2.0",
    "react/jsx-runtime": "https://esm.sh/react@18.2.0/jsx-runtime",
    "react-dom": "https://esm.sh/react-dom@18.2.0",
    "react-dom/client": "https://esm.sh/react-dom@18.2.0/client"
  }
}
</script>
<script type="text/babel" data-type="module">
  import React, { useState, useEffect, useRef, useMemo } from "https://esm.sh/react@18.2.0";
  import { createRoot } from "https://esm.sh/react-dom@18.2.0/client";
  // ... Firebase imports
</script>
```

> ⚠️ Babel 버전 고정 + importmap 둘 다 필수. 안 하면 흰 화면.

---

## 3. Firebase 신규 설정 (선생님이 미리 준비)

1. https://console.firebase.google.com 에서 **새 프로젝트 생성** (이름 예: `w-program-2`)
2. **Firestore Database** 생성 (asia-northeast3 / production mode)
3. **Authentication** 활성화:
   - Sign-in method → Anonymous 켜기
   - Sign-in method → Email/Password 켜기
   - Users → 선생님 계정 생성 (이메일 + 강력한 비번)
4. Project Settings → 웹 앱 추가 → **firebaseConfig 발급**
5. 새 채팅에 firebaseConfig 전달

### Firestore 보안 규칙 (배포 시 적용)

선생님 이메일 부분만 채워서 Firestore → Rules에 붙여넣을 것:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isTeacher() {
      return request.auth != null && request.auth.token.email == 'TEACHER_EMAIL_HERE';
    }
    function isAuthed() {
      return request.auth != null;
    }

    match /classData/mainState   { allow read: if true;       allow write: if isTeacher(); }
    match /schedules/{week}       { allow read: if isAuthed(); allow write: if isTeacher(); }
    match /evaluations/{date}/{eid}  { allow read: if isAuthed(); allow write: if isAuthed(); }
    match /groupVotes/{date}/{eid}   { allow read: if isAuthed(); allow write: if isAuthed(); }
    match /dailyScores/{date}/{sid}  { allow read: if isAuthed(); allow write: if isTeacher(); }
    match /biweeklyScores/{p}/{sid}  { allow read: if isAuthed(); allow write: if isTeacher(); }
    match /coinTxns/{id}             { allow read: if isAuthed(); allow create: if isAuthed(); allow update, delete: if isTeacher(); }
    match /studentAuth/{doc}         { allow read: if isAuthed(); allow create, update: if isAuthed(); allow delete: if isTeacher(); }
    match /_sessions/{doc}           { allow read: if isTeacher(); allow create: if isAuthed(); allow update, delete: if false; }
  }
}
```

---

## 4. 학급 구조

| 구분 | 인원 | 비고 |
|---|---|---|
| 의장 | 5명 (학기초 고정) | 회장단 4명 + 와일드카드 1명 |
| 위원 | 20명 | 매주 모둠/역할 로테이션 |
| 모둠 | 5모둠 × 5명 | 의장 1 + 위원 4 |
| 역할 | 의장(소통) + 위원 4역할 | 질서·학습·건강·행정 |
| 기간 | 21주 | 2026-08-17 ~ 2027-01-08, 방학 제외 |

### 학생 명단 (25명, 1학기와 동일)

```javascript
const genderMap = {
  "가동민": "M", "김선유": "M", "김수형": "M", "김예원": "F", "김용훈": "M",
  "김원준": "M", "김주아": "F", "김찬우": "M", "김하영": "F", "문서우": "F",
  "박찬": "M",   "배한민": "M", "유선재": "F", "윤재익": "M", "이다인": "F",
  "이서이": "F", "이서희": "F", "이채연": "F", "임유나": "F", "정지수": "M",
  "조수아": "F", "조이환": "M", "최지완": "M", "한민종": "M", "홍아영": "F"
};
```

### 의장 5명 (학기초 결정, 1년 고정)

| 모둠 | 의장 | 역할 |
|---|---|---|
| 1모둠 | 윤재익 | 반장 1 |
| 2모둠 | 김하영 | 반장 2 |
| 3모둠 | 가동민 | 부반장 1 |
| 4모둠 | 홍아영 | 부반장 2 |
| 5모둠 | 이다인 | 와일드카드 |

(선생님이 학기 초 변경 가능)

---

## 5. 핵심 알고리즘 명세

### 5.1 21주 자리 사전 계산 (학기 시작 전 1회)

```
[입력]
  - 25명 (의장 5 + 위원 20)
  - 각자 성별
  - 21주

[처리]
  1. 위원 20명 초기 (모둠 0~4, 역할 0~3) 배정
     → 5×4 그리드 채움. 각 역할마다 5명씩.
  2. 21주 초기 스케줄
     - 1~4주: 직교 라틴 방진
       · 역할 진화: roleIdx = (initial + week) % 4
       · 모둠 점프: groupIdx = (initial + jumpStep * week) % 5
         (질서:+1, 학습:+2, 건강:+3, 행정:+4)
     - 5~21주: 변형 패턴 + 무작위 시드
  3. 시뮬레이티드 어닐링 (10,000회)
     cost = α × Σ pairCount[i][j]²
          + β × Σ (성별 4:0 또는 0:4 모둠 수)
          + γ × Σ 같은 모둠 연속 횟수
          + δ × Σ 같은 역할 연속 횟수
          + ε × Σ 역할 횟수 학생별 표준편차
     제안 가중치: α=10  β=5  γ=3  δ=3  ε=2
     온도: 1.0 → 0.01 지수 감쇠
     랜덤 swap → cost 비교 → 어닐링 확률 채택
  4. schedules/1 ~ schedules/21 → Firestore 저장

[출력]
  학생/교사가 21주 전체 캘린더 미리 보기
```

### 5.2 날짜 기반 자동 자리 전환

```
앱 로드 시:
  weekNum = floor((오늘 - 학기시작일 "2026-08-17") / 7) + 1
  weekNum = clamp(weekNum, 1, 21)
  현재 표시 = schedules/{weekNum}
  → 별도 조작 없이 날짜만 지나면 자리 자동 전환
```

### 5.3 교사 수동 변경

```
용도:
  - 임시 자리 교체
  - 학생 코인 사용 → 교사가 수동 swap (코인 시스템은 기존 그대로)

UI:
  교사 화면 → 자리 관리 탭
  → 특정 weekNum에서 두 학생 선택 → swap
  → manualOverrides 배열에 기록 (원복 가능)

영향 범위:
  - 해당 주차의 schedules/{n}에만 반영
  - 다른 주차 영향 없음
```

### 5.4 모둠 내 평가 (단계 1, 매일)

```
방식: 각 학생이 같은 모둠 다른 4명을 본인 역할 관점에서 평가
척도: +1 / 0 / -1   (1학기 시스템과 동일, 교사 조정 가능)

매트릭스 (역할 관점):
  의장이 위원 4명 평가 (리더십·참여도)
  질서 지킴이가 4명 평가 (질서)
  학습 지킴이가 4명 평가 (학습)
  건강 지킴이가 4명 평가 (건강)
  행정 지킴이가 4명 평가 (책임)

저장:
  evaluations/{date}/{evaluatorId} = { targetId: +1|0|-1, ... 최대4명 }

집계:
  학생 j의 그 날 모둠내_점수 = 받은 4개 평가의 합 (-4 ~ +4)

교사 조정:
  특정 학생/날짜 점수를 수동 보정 가능
```

### 5.5 모둠 간 평가 (단계 2, 매일)

```
방식: 각 학생이 본인 모둠 제외 4모둠을 평가 (학생들끼리)
척도: [확인 필요 — 새 채팅에서 결정]
  (A) +1 / 0 / -1
  (B) 1위만 투표 (한 모둠 선택)
  (C) 1~3점
  추천: (A) — 모둠내 평가와 일관성

저장:
  groupVotes/{date}/{evaluatorId} = { groupId: +1|0|-1, ... 4모둠 }

집계:
  1. 각 모둠 그 날 점수 = 본인 모둠 제외 20명이 매긴 평가 합
  2. 5개 모둠을 점수 내림차순 정렬
  3. Dense Ranking (동점 시 후순위 안 내림)
     예) 점수 9,7,7,5,3 → 순위 1,2,2,3,4
     예) 점수 8,8,6,6,4 → 순위 1,1,2,2,3
  4. 순위 → 개인 점수
     1위→5, 2위→4, 3위→3, 4위→2, 5위→1
  5. 같은 모둠 5명 전원에게 동일 점수 (의장 포함)

저장:
  dailyGroupRankScores/{date}/{studentId} = 1~5
```

#### Dense Ranking 의사코드

```
function denseRank(groupScores) {
  const sorted = Object.entries(groupScores).sort((a, b) => b[1] - a[1]);
  const ranks = {};
  let rank = 0, prev = null;
  for (const [gId, score] of sorted) {
    if (score !== prev) { rank++; prev = score; }
    ranks[gId] = rank;
  }
  return ranks;
}
function toPersonalPoint(rank) { return Math.max(0, 6 - rank); } // 1→5 ... 5→1
```

### 5.6 일일 점수 집계

```
각 학생 그 날 점수 =
    모둠내_점수      (-4 ~ +4)
  + 모둠간_점수      ( 1 ~ +5)
  + 교사_보너스      (선택)
```

### 5.7 격주 MVP & 코인 (금요일 16:00)

```
시점: 격주 금요일 16:00 (2주에 1번 결산)
대상: 전체 25명 (의장 포함)
기준: 직전 2주간 일일 점수 합
선정: 상위 5명 (동점자 모두 포함 → 6명 이상도 OK)
보상: 코인 1개 자동 지급 (기존 코인 시스템에 누적)
```

### 5.8 코인 자리변경 (기존 시스템 유지)

```
프로세스:
  1. 학생: 코인 사용해서 변경하고 싶은 (주차, 모둠, 역할) 신청
     마감: 일요일 23:59
     선착순, 환불 없음, 연속 잠금 무제한
  2. 교사: 신청 목록 확인 → 5.3 '교사 수동 변경'으로 swap
  3. 그 주차만 적용, 다음 주 영향 없음
```

### 5.9 학생 변동 처리

```
[전출] 그 학생의 모든 주차 자리 → "빈자리" 마킹
[전입] 새 학생을 빈자리 중 하나에 자동 또는 교사 지정으로 배치
[의장 변동] 거의 없지만, 비면 교사가 새 의장 지정 (스케줄 재계산 X)
```

---

## 6. 데이터 모델 (Firestore)

```
students/{id}
  - name, gender, isChair, chairOfGroup, isWildcard
  - silverCoins

schedules/{weekNum}    # 1~21
  - weekStart: "2026-08-17"
  - groups: [
      { groupId: 1, chair: studentId,
        members: [{ studentId, role: "질서" }, ..., { studentId, role: "행정" }]
      }, ...
    ]
  - manualOverrides: [{ swappedAt, studentA, studentB, byTeacher }]

evaluations/{date}/{evaluatorId}
  - { targetId: +1|0|-1, ... }   # 모둠 내, 최대 4명

groupVotes/{date}/{evaluatorId}
  - { groupId: +1|0|-1, ... }    # 모둠 간, 4모둠

dailyScores/{date}/{studentId}
  - peerScore, groupRankScore, bonus, total

biweeklyScores/{period}/{studentId}
  - sum, isMVP, mvpRank

coinTxns/{id}
  - studentId, type ("mvp"|"spend"|"manual"), amount, date, note

studentAuth/{studentId}
  - hash, updatedAt  # 학생 본인 비번 변경 (1학기와 동일)

_sessions/{id}
  - 활동 로그 (audit) — 1학기와 동일 구조
```

---

## 7. 우선순위별 작업 순서 (Phase)

> 새 채팅에서 이 순서대로 진행

```
Phase 1  🔐 기본 인프라
         - HTML 골격 (importmap, Babel 7.23.10 고정)
         - Firebase 초기화, 로그인 (교사 이메일/비번 + 학생 익명)
         - 학생 명단 + 의장 5명 입력
         - Firestore 보안 규칙 배포

Phase 2  🪑 21주 사전 자리 배치
         - 시뮬레이티드 어닐링 구현
         - 페어 분포 시각화 (선생님 검증용)
         - schedules/1~21 저장
         - 학기 시작 전에 1회 실행

Phase 3  📅 날짜 자동 전환 + 교사 수동 변경
         - 현재 주차 자동 계산 + 화면 표시
         - 교사용 수동 swap UI
         - 학생/교사 캘린더 (21주 미리보기)

Phase 4  🎭 모둠 내 역할 시스템
         - 5역할 표시 + 역할별 책임 설명
         - (1학기의 25개 1인1역 모두 제거)

Phase 5  ⭐ 2단계 평가 (매일)
         - 단계 1: 모둠 내 +1/0/-1
         - 단계 2: 모둠 간 평가 → Dense Ranking → 개인 점수
         - 학생/교사 UI

Phase 6  🏆 격주 MVP + 코인 자동 지급
         - 2주 누적 집계
         - 금요일 16:00 자동 계산
         - 코인 보상 트랜잭션

Phase 7  🪙 코인 자리변경 (기존 시스템 + 교사 수동)
         - 학생 신청 UI
         - 교사 처리 화면

Phase 8  🐢 (선택) 1학기 거북이 데이터 가져오기
         - JSON 파일 업로드 → 새 DB에 import
         - 학생별 권수만 가져오거나, 감상문까지 가져오거나 선택
```

---

## 8. 1학기에서 얻은 교훈 (꼭 지킬 것)

### 8.1 Firebase 읽기 비용
- 무료 한도: **하루 5만 회**
- 25명 학급이 1학기 동안 종종 한도 도달
- **금지**: `onSnapshot`으로 전체 컬렉션 실시간 구독
- **권장**:
  - 탭 진입 시 1회만 로드 (`loadedRef` 가드)
  - 본인 쓰기 후엔 로컬 state만 갱신 (재조회 X)
  - 글 열람 중인 1개 문서만 실시간 구독

### 8.2 CDN 자동 업그레이드 위험
- `@babel/standalone` 최신 추적 시 자동 JSX runtime이 켜져 흰 화면
- **반드시** `@babel/standalone@7.23.10` 같이 **버전 고정**
- importmap으로 `react/jsx-runtime` 매핑 추가 (이중 안전망)

### 8.3 Apps Script 배포 반복 작업
- GitHub 푸시 ≠ 배포
- 매번 Apps Script 편집기에서 코드 교체 + "New version → Deploy" 필요
- 사용자 안내 메시지에 매번 이 단계 명시

### 8.4 익명 인증 + 보안 규칙
- 학생은 익명 Firebase Auth
- 교사는 이메일/비번 Firebase Auth → 규칙에서 `email == '...'`로 구분
- 학생 비번은 SHA-256 해시 후 `studentAuth/{id}`에 저장 (1학기 도입)

### 8.5 데이터 무결성
- `JSON.parse(JSON.stringify(...))` 대신 `structuredClone()` 사용
- 폴백 코드의 하드코딩(`{[id]: 1}` 같은 것)은 위험 — 현재값 반영
- 모든 try/catch에 toast 에러 표시 (silent fail 금지)

---

## 9. 결정 안 된 사항 (새 채팅에서 확정 필요)

| # | 항목 | 후보 | 추천 |
|---|---|---|---|
| 1 | 모둠 간 평가 척도 | +1/0/-1 / 1위 투표 / 1~3점 | **+1/0/-1** |
| 2 | 모둠 간 평가 시점 | 매일 종회? 주 1회? | **매일 종회** |
| 3 | 교사 보너스 부여 방식 | 즉시 +/- 입력 / 일괄 입력 | 즉시 |
| 4 | 학기 시작일 확정 | 2026-08-17 | 확정 |
| 5 | 학기 종료일 확정 | 2027-01-08 | 확정 |

---

## 10. UI 시나리오 가이드

### 학생 화면
- **홈**: 이번 주 본인 모둠/역할/의장/모둠원 표시
- **캘린더**: 21주 전체 본인 일정 미리보기 (코인 잠금 신청 진입점)
- **평가**: 매일 종회 시간에 활성화 (모둠 내 4명 + 모둠 간 4모둠)
- **점수**: 본인 일일/누적 점수, MVP 누적, 코인 잔액
- **비번 변경**: 우측 상단 🔑 버튼

### 교사 화면
- **대시보드**: 오늘 모둠 현황, MVP 후보, 코인 신청 대기
- **자리 관리**: 21주 캘린더 + 수동 swap
- **평가 보정**: 일일 점수 수정, 보너스 부여
- **사전 계산 실행**: 학기 초 1회 (시뮬레이티드 어닐링 트리거)
- **페어 분포**: 25×25 만남 매트릭스 시각화
- **활동 로그**: `_sessions` 조회 (1학기와 동일 함정)
- **거북이 import**: 1학기 JSON 백업 불러오기 (선택)

---

## 11. 1학기 데이터 (참고용)

- 거북이 독서 백업 JSON: 별도 보관 중
  - 메타: 2026-06-22 시점 백업
  - 감상문 125건 (정식 112건)
  - 학생 25명 명단
  - booksRead: mainState 17명 + sharedRecords 2명 (선생님 수동 보정 포함)
- 2학기 import 시 활용 가능

---

## 12. 새 채팅 시작 첫 메시지 템플릿

```
이 문서대로 2학기 학급 자치 시스템을 새로 만들어줘.

[조건]
- 기존 w-program Firebase 절대 건드리지 않음
- 새 Firebase 프로젝트: w-program-2 (firebaseConfig 아래 첨부)
- Apps Script 배포 환경
- React 18 + Babel 7.23.10 고정 + importmap
- 단일 HTML 파일

[firebaseConfig]
{
  apiKey: "...",
  authDomain: "w-program-2.firebaseapp.com",
  projectId: "w-program-2",
  ...
}

Phase 1부터 순서대로 가자. 우선 Phase 1: 기본 인프라부터.
```

---

**문서 작성일**: 2026-06-22
**최종 검토**: 1학기 종료 시점
**관련 리포**: https://github.com/codechord123/smartclass-claude-/blob/claude/fix-post-loading-issue-1HWBo/2학기-신규개발-인수인계.md
